using System;
using System.Collections.Generic;
using System.Text;

namespace ReinPractice.OPPs
{
   /*class CarDetails
    {
        string colour;
        string name;
        double price;
        CarDetails(string red, string bmw, double price)
        {
            this.colour = red;
            this.name = "bmw";
            this.price = 5000000;
        }
        void SwapByRef(ref double p1, ref double p2)
        {
            Console.WriteLine("before colouring car" + p1 + " " + p2);
            double temp = p1;
            p1 = p2;
            p2 = temp;
            Console.WriteLine("After coluring of car" + p1 + "" + p2);
        void SwapByValue(double p1, double p2)
        {
            Console.WriteLine("before colouring car" + p1 + " " + p2);
            double temp = p1;
            p1 = p2;
            p2 = temp;
            Console.WriteLine("After coluring of car" + p1 + "" + p2);
            }
            void Run()
            {
                Console.WriteLine("Run Car in normal speed");
            }
            static void Main(string[] args)
            {
                CarDetails ed = new CarDetails("red","bmw",5000000);
                Console.WriteLine("enter p1");
                double p1 = Convert.ToInt32(Console.ReadLine());
                Console.WriteLine("enter p1");
                double p2 = Convert.ToInt32(Console.ReadLine());
                ed.SwapByRef(ref p1, ref p2);
                    Console.WriteLine("ater swap in main" + p1 + " " + p2);

               // ed.SwapByValue( 10, 20);
               //      Console.WriteLine("ater swap in main" + p1 + " " + p2);
                    


            }
        }
    }*/
}
